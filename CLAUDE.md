# CLAUDE.md

## 原則
* シンプルなコードで実装すること
  * YAGNI原則、KISS原則、DRY原則を守ること
  * 過剰設計、オーバーエンジニアリングにならないように注意すること
* 実装・テストとも、外側の処理(呼び出す側)を先に書き、内部で使われるヘルパー関数を後に書くこと(Step-down Rule)
* コメント・ドキュメントには結論のみを簡潔に書き、そこに至った議論の経緯や理由の羅列は書かないこと

## 使用言語
* ClaudeCodeでの会話や、ソースコードに記載するコメントはすべて日本語で会話すること

## ドキュメンテーション
* 箇条書きをベースに簡潔に記載すること

## コミット・ブランチ運用
* ブランチ戦略は`main`ブランチ + 作業ブランチのシンプルな構成とする
  * `main`: 常にデプロイ可能な状態を保つ
  * 作業ブランチ: `main`から切り、作業完了後は`main`にマージして削除する
* コミットメッセージは`<type>: <説明>`の形式にすること(type: `feat`/`fix`/`chore`)
* コミットメッセージは1行で完結させ、40字程度に収めること(本文は原則つけない)
* ブランチ名は`<type>/<内容>`形式にすること(例: `feat/add-login`)

## パッケージの管理
* パッケージを追加・削除する際は`pyproject.toml`を直接編集せず、`uv add <package>`/`uv remove <package>`を使うこと(`uv.lock`との不整合を防ぐため)

## テスト方針
* テストケースは正常系、準正常系、異常系の3観点で考えること
  * 正常系: 正しい入力に対する、期待通りの動作
  * 準正常系: 入力は正しくないが、設計者が想定し、仕様として作り込んだエラー処理が働くケース
  * 異常系: 設計者が想定していなかった不正な状態が起きるケース
* 特に異常系については全網羅を目指さず、発生確率、被害規模を踏まえてテストケースを考えること
* 実装が一区切りついたら`uv run pytest`を実行し、結果を確認すること

## テストの書き方
* 各テストには簡潔なdocstringを書き、「何を検証しているか」と「正常系・準正常系・異常系のどれに該当するか」を記載する
* テスト実装の際は、まず初めにdocstringsのみを書いた空のテストケースを作り、実装前に開発者と合意をとること
* 各テストにはAAA(Arrange / Act / Assert)パターンをコメントで明示すること

## Terraform
* 各ブロックに対して、何のためのコードかを1行でコメントすること
  * `description`などが記載されており、コード自体でその内容がわかるようになっていればコメントは不要